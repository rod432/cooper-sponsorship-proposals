# Supabase project credentials.
# Get these from your Supabase project Settings → API.
NEXT_PUBLIC_SUPABASE_URL=
# Either the new publishable key (sb_publishable_...) or the legacy anon JWT.
NEXT_PUBLIC_SUPABASE_ANON_KEY=
